{
  "name": "vortex-lang-wasm-www",
  "version": "0.1.0",
  "description": "Web REPL for Vortex Lang using WASM and xterm.js",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build --target web",
    "dev": "vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "xterm": "^5.3.0"
  },
  "dependencies": {
  }
}
